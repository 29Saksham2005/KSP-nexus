# Web Framework & Server
fastapi==0.109.2
uvicorn[standard]==0.27.1

# Database & ORM
sqlalchemy==2.0.25
psycopg2-binary==2.9.9

# Authentication & Security (JWT & Hashing)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# Environment Variables & Validation
pydantic==2.6.1
pydantic-settings==2.1.0
python-dotenv==1.0.1

# Optional but recommended for API request handling (CORS)
# (FastAPI includes Starlette, but explicitly defining versions ensures stability)
starlette==0.36.3